const fs = require('fs'); //filesystem

module.exports = {
    data: connect()
};

function connect() {
    //need able to test connection to github 
    //get username from main:config
    return data;
}